node_modules/
dist/
.env
*.log
.DS_Store
desktop/renderer-dist/
desktop/native/dictate.app/
desktop/native/dictate

# 代理工作记忆（.workbuddy/memory/），属于本地笔记，不进版本库
.workbuddy/
# Control-Agent 运行时本地状态：模型配置（config.json，含 apiKey！）、
# 会话历史（sessions/，--resume 用）——与 .c-agent/ 同类，绝不进版本库
.control-agent/
.c-agent/
# c-agent 跨会话记忆（memory 工具写入项目根，本地工作笔记不进版本库）
/MEMORY.md
# bot 平台依赖的独立安装目录（wechaty 树大，独立装避免污染主 lockfile；
# 通过 symlink 从主 node_modules 引用，见 src/bot/doc/README.md）
/bot-deps/
# browser-use connector 的持久 Python venv（见 connectors-mcp/browser-use/index.ts）
/browser-use-venv/
# wechaty 登录 cookies（wx.qq.com 会话凭据，绝不能进版本库）
c-agent-wechat.memory-card.json
