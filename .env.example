# 复制为 .env 后填入真实值。两个都不填时自动使用 mock 适配器，开箱即可运行。
#
# ⚠️ 本文件是模板，会被提交到 git。真实密钥请填到 .env（已被 .gitignore 忽略），
#    不要写在这里 —— 一旦提交就会永久留在 git 历史里。

MODEL=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# 可选：覆盖默认模型与端点
#
# OPENAI_BASE_URL 只填到版本目录即可，代码会自动拼上 /chat/completions。
# 写成 https://host/v1/chat/completions 会变成 .../chat/completions/chat/completions 而 404。
# MODEL=gpt-4o-mini
# OPENAI_BASE_URL=https://api.openai.com/v1

# ANTHROPIC_BASE_URL=https://api.anthropic.com
